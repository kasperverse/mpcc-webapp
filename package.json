{
  "name": "webapp",
  "type": "module",
  "scripts": {
    "dev": "wrangler pages dev public --ip 0.0.0.0 --port 3000",
    "dev:sandbox": "wrangler pages dev public --ip 0.0.0.0 --port 3000",
    "build": "echo 'Static SPA - no build step required'",
    "preview": "wrangler pages dev public",
    "deploy": "wrangler pages deploy public --project-name mpcc",
    "cf-typegen": "wrangler types --env-interface CloudflareBindings"
  },
  "dependencies": {
    "hono": "^4.12.12"
  },
  "devDependencies": {
    "@hono/vite-build": "^1.2.0",
    "@hono/vite-dev-server": "^0.18.2",
    "vite": "^6.3.5",
    "wrangler": "^4.4.0"
  }
}